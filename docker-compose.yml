services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: open_wallet_auth
      POSTGRES_USER: open_wallet_auth
      POSTGRES_PASSWORD: open_wallet_auth
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U open_wallet_auth -d open_wallet_auth"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
